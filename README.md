# cvmfs-github-action-test

Very simple demonstration of a GitHub action that reads a file from cvmfs and does some verification. In this case, it verifies that the `count_events.C` macro returns the same number of events every time.

